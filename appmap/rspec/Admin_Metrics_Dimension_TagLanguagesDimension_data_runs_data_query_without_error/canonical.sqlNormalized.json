[
  "SELECT COALESCE(statuses.language, ?) AS language, count(*) AS value FROM statuses INNER JOIN statuses_tags ON statuses_tags.status_id = statuses.id WHERE statuses_tags.tag_id = ? AND statuses.id BETWEEN ? AND ? GROUP BY COALESCE(statuses.language, ?) ORDER BY count(*) DESC LIMIT ?"
]